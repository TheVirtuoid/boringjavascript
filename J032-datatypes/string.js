const myDoubleQuotes = "Insert 'Single' Quote";
const mySingleQuotes = 'Insert "Double" Quote';
const myTemplate = `Combination: (${myDoubleQuotes}) - (${mySingleQuotes})`;

console.log(`\nmyDoubleQuotes is type '${typeof myDoubleQuotes}' (${myDoubleQuotes})`);
console.log(`mySingleQuotes is type '${typeof mySingleQuotes}' (${mySingleQuotes})`);
console.log(`myTemplate is type '${typeof myTemplate}' (${myTemplate})`);

