import printBinary from "./printBinary.js";

const myNumber = -4;
const rightShiftNumber = myNumber >> 1;

console.log(`myNumber ${myNumber}\n${printBinary(myNumber)}\n`);
console.log(`rightShiftNumber ${rightShiftNumber}\n${printBinary(rightShiftNumber)}`)

